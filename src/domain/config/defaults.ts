// Storage key for the persisted CashFlowProfile (Phase 1).
// Phase 2 will extend this file with mortgage/scoring defaults
// (interest rate, term, DSR cap, ratio weights, etc.) — keep it minimal for now.
export const PROFILE_STORAGE_KEY = "bear-tung:profile";
